# Dependencies for the evaluation pipeline (evaluation/eval_manual_auto_ROIs.ipynb).
# Install into the same environment as the main NEUSEG requirements:
#     python -m pip install -r evaluation/requirements.txt

# PDNL command-line tools invoked as subprocesses by Helper_Functions/pipeline_ao.py.
# These are resolved from the active environment's bin/ directory, so they must be
# installed in the same env the notebook runs in.
pdnl_extract==0.7
pdnl_process==0.5
pdnl_aggregate==0.4

# Used by the evaluation helpers on top of the base NEUSEG requirements.
pandas
openslide-python
shapely
